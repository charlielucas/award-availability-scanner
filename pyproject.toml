[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "award-availability-scanner"
version = "0.1.0"
description = "Normalize and rank award availability records from provider-style API payloads."
requires-python = ">=3.9"
